import Modal from "../templates/Modal";

export default class ContactModal extends Modal {
  constructor() {}
  createContactForm() {
    /*
     <form class="modal__form">
          <div>
            <label id="firstname" for="firstname">Prénom</label>
            <input
              id="firstname"
              aria-labelledby="firstname"
              type="text"
              required
            />
            <label id="lastname" for="lastname">Prénom</label>
            <input
              id="lastname"
              aria-labelledby="lastname"
              type="text"
              required
            />
            <label id="email" for="email">Email</label>
            <input id="email" aria-labelledby="email" type="email" required />
            <label id="message" for="message">Votre Message</label>
            <textarea
              id="message"
              aria-labelledby="message"
              required
            ></textarea>
          </div>
          <button
            aria-label="Soumettre le formulaire de contact"
            class="contact_button"
          >
            Envoyer
          </button>
        </form>
        */
  }
}
